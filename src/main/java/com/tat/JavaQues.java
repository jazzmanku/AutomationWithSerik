package com.tat;

public class JavaQues {
    public static boolean checkIn7090(int a, int b){
        if(a >= 70 && a <=80 && b>=70 && b<=80)
            return  true;
        if (a >= 80 && a<=90 && b>=80 && b<=90)
            return  true;
        return false;
    }
}
